plugins {
  id "org.sonarqube" version "4.3.1.3277"
}

sonar {
  properties {
    property "sonar.projectKey", "chamiz_dirtycode"
    property "sonar.organization", "chamiz"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}
